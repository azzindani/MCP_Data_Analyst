name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Install uv
        uses: astral-sh/setup-uv@v6

      - name: Install dependencies
        run: |
          cd servers/data_basic && uv sync --all-packages --dev

      - name: Lint with ruff
        run: |
          uvx ruff check servers/ shared/ tests/ --exclude "**/.venv/**"

      - name: Run tests
        run: |
          cd servers/data_basic && uv run pytest ../../tests/ -v --tb=short
