name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Install uv
        uses: astral-sh/setup-uv@v6

      - name: Install dependencies
        run: |
          cd servers/data_basic && uv sync --all-packages --dev
          cd ../data_advanced && uv sync --all-packages --dev
          uv pip install pytest pytest-cov

      - name: Lint with ruff
        run: |
          uvx ruff check servers/ shared/ tests/ --exclude "**/.venv/**"

      - name: Run tests
        run: |
          PYTHONPATH=. uv run pytest tests/ -v --tb=short
