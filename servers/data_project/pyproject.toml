[project]
name = "data-project"
version = "0.1.0"
requires-python = "==3.12.*"
dependencies = [
    "fastmcp>=2.0,<3.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["servers/data_project"]
