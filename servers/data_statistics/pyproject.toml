[project]
name = "data-statistics"
version = "0.1.0"
requires-python = "==3.12.*"
dependencies = [
    "fastmcp>=2.0,<3.0",
    "pandas>=2.2",
    "scipy>=1.10",
    "statsmodels>=0.14",
    "plotly>=5.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
